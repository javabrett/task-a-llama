# Secrets — never committed
.env
.env.local
config.yml

# Runtime state — bind-mount targets for Docker
db/
files/
backups/

# SQLite sidecar files (write-ahead log, shared memory)
*.db
*.db-journal
*.db-wal
*.db-shm

# macOS noise
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary / scratch
*.tmp
*.bak
scratch/

repos/
